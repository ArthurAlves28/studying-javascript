// Declaração padrão de variável
var a = 3 // var permite que a mesma variável seja redeclarada em um ponto futúro do código

// Há diferença entre let e var
let b = 4 // let não permite que a mesma variável seja redeclarada em um ponto futúro do código

var a = 30
b = 40

console.log(a, b)

a = 300
b = 400

console.log(a, b)

// Declaração de constante
const c = 5

console.log(c)